- name: test
  hosts: all
  gather_facts: false

  tasks:
    - debug: var=inventory_hostname
    # - debug: var=group_names
    - debug: var=zzz_all_test_value
    - debug: var=all_company_code
    # - debug: var=hostvars[inventory_hostname]
